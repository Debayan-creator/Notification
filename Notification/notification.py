import time
from plyer import notification
if __name__=="__main__":
    notification.notify(
        title="PLease Drink Water",
        message="Bsdk.....",
        timeout=10
    )
    time.sleep(60*60)
